spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
  r2dbc:
    url: r2dbc:mssql://localhost:1433/GestionInventarioDB
    username: giia_user
    password: giia2025
  application:
    name: proveedores-service

server:
  port: 8082


datacache:
  redis:
    key-prefix:
      proveedor: "proveedor:"
      usuario-proveedor: "usuario-proveedor:"
      proveedor-login: "provedor-login:"
    channel:
      proveedor: "proveedor-updates"
      usuario-proveedor: "usu-proveedor-updates"
